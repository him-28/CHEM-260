#include <stdlib.h>
#define MAXSIZE 1000000
#define TIMES 1000
main() {
  int *array1;
  int array2[MAXSIZE];
  int i,times=TIMES,t;
  array1=(int*)malloc(MAXSIZE*sizeof(int));
  for (t=0; t<TIMES; t++) {
    for (i=0; i<MAXSIZE; i++) {
	/* Insert 2 lines here */
    }
  }
}
  
