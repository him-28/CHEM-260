#include <stdio.h>
#include <stdlib.h>
main() {
  int *array;
  /* Insert line here */
  while (1) {
    /* Insert line here */
    if (array==NULL) {
      printf("Failed to allocate %ld ints\n",n);
      exit(1); }
    printf("Successfully allocated %ld ints\n",n);
    /* Insert line here */
    n*=10;
  }
}
