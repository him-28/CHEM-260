#include <stdio.h>  /* for file i/o functions */
#include <stdlib.h> /* for exit() function */
main() {
  int i, j, k;
  /* Add 2 lines */
  if (inputfp==NULL) {
    printf("Trouble, could not open input.txt\n");
    exit(1);
  }
  /* Add 1 line */
  printf("Numbers read in: i=%d j=%d k=%d\n",i,j,k);
}
